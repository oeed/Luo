local one_func = function()
print("go")
end
local functions = {
	one = one_func,
}

local t1 = {
	one = function()
		return one_func
	end,
	two = function() return "hi" end
}

local v = 5435
local n = 100000000
local start1 = os.clock()
local one = "one"
for i = 1, n do
	local g = i % 2 == 1 and "one" or "two"
	local _ = t1[g]()
end
local total1 = os.clock() - start1
local start2 = os.clock()
for i = 1, n do
	local g = i % 2 == 1 and "one" or "two"
	local func = functions[g]
	if func then
	else
		local _ = t1[g]
	end
end
local total2 = os.clock() - start2

print("Total 1:" .. total1)
print("Average 1:" .. total1 / n)
print("Total 2:" .. total2)
print("Average 2:" .. total2 / n)